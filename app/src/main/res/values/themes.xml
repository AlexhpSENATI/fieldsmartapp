<resources xmlns:tools="http://schemas.android.com/tools">

    <style name="Base.Theme.FieldSmart" parent="Theme.Material3.DayNight.NoActionBar">

        <!-- Desactiva colores dinámicos -->
        <item name="android:forceDarkAllowed">false</item>

        <!-- Barra de estado (hora, señal, batería) -->
        <item name="android:statusBarColor">@color/moradoxd</item>
        <item name="android:windowLightStatusBar">false</item>

        <!-- Barra de navegación (parte inferior del sistema) -->
        <item name="android:navigationBarColor">@color/celeste</item>

    </style>

    <style name="Theme.FieldSmart" parent="Base.Theme.FieldSmart" />
</resources>
